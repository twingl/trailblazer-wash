/** @jsx React.DOM */
var React = require('react');
var Button = require('app/components/button');
var Popover = require('app/components/popover');

module.exports = React.createClass({
  render: function () {

    var content = "Your map is now public and viewable at:"

    return  <div>
              <Popover display={this.props.display}>
                <div className="shared-message" >
                  {content}
                </div>
                <divclassName="shared-url" >
                  {this.props.url}
                </div>
              </Popover>
              <Button 
                className="share-button"
                onClick={this.props.shareAction}
                title={this.props.title} />
            </div>
  },





})